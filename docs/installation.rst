.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install tummy_time

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tummy_time
    $ pip install tummy_time
